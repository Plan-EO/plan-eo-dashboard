replicaCount: 1

image:
  repository: ghcr.io/plan-eo/dashboard:prod-1.0.0
  tag: latest
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-regcred

service:
  type: ClusterIP
  port: 80
  targetPort: 4000

resources:
  limits:
    cpu: 1
    memory: 2Gi
  requests:
    cpu: 0.5
    memory: 1Gi